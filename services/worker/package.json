{
  "name": "@dolcove/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build --workspace @dolcove/shared && tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@dolcove/shared": "file:../../packages/shared"
  }
}